{:paths ["src"]
 ;; langgraph brings langchain transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the kotoba-lang org of this workspace. This repo is standalone
 ;; (forkable outside the workspace); the :local/root path only resolves
 ;; inside the monorepo checkout -- a standalone fork should override it
 ;; with git coordinates (see the "Standalone Use" section in README.md).
 ;;
 ;; FIX (was `:deps {}` with the real coordinate only reachable via the
 ;; :dev alias's :override-deps): `:override-deps` REPLACES an existing
 ;; dependency already present in the resolved dep graph -- it does not
 ;; ADD a new one. With `:deps {}` there was nothing named
 ;; io.github.kotoba-lang/langgraph to override, so the override was
 ;; inert on every path (`clojure -M:run`, `-M:test`, plain `-M`) and no
 ;; real langgraph-clj StateGraph ever compiled or ran. Moving the real
 ;; coordinate to top-level :deps (the actually-used path) fixes this;
 ;; :dev now only does what its name says -- pin the *transitive*
 ;; langchain dependency to the local workspace checkout.
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}}
 :aliases
 {;; offline workspace dev -- pin transitive langchain to local checkout
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "forestrysupport.sim"]}
  :render-html {:main-opts ["-m" "forestrysupport.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
